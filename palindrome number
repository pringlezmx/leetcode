#python 3

class Solution:
    def isPalindrome(self, x: int):
        num = x
        rnum = str(num)[::-1]
        if x < 0:
            return False
        elif int(rnum) == num:
            return True
        else:
            return False
